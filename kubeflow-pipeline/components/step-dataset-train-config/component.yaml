name: Train Dataset Generation
description: Genera il dataset di training

implementation:
  container:
    image: localhost:5001/step-dataset-generation-config:kf
    command: [
      python,
      'entrypoint.py',
      -type,
      train
    ]